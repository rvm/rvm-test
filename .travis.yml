language: ruby
rvm:
  - 1.9.3
before_install:
  - unset BUNDLE_GEMFILE
  - rvm get head
  - rvm alias delete default
  - hash -r
install: gem install dtf
script: 'NOEXEC=skip dtf --text fast/*'
notifications:
  irc:
    channels:
      - "irc.freenode.org#rvm-test"
      - "irc.freenode.org#rvm"
  email:
    recipients:
      - mpapis@gmail.com
      - me@deryldoucette.com
    on_failure: change
